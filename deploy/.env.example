# Required: upstream OpenAI-compatible base URL, must include /v1
UPSTREAM_BASE_URL=https://api.openai.com/v1

# Required: upstream API key (kept on server, never exposed to browser)
UPSTREAM_API_KEY=sk-your-upstream-key

# Recommended: client access code (this is what the browser/kelivo uses)
ACCESS_CODE=change-me

# Optional: max upload size in MiB (default 25)
MAX_UPLOAD_MB=25

# Optional: only needed if you DON'T use nginx same-origin proxy
# CORS_ALLOW_ORIGINS=https://your-frontend-domain.com
CORS_ALLOW_ORIGINS=
